{
  "name": "constitutional-replay-v1",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first constitutional replay kernel for AL receipts.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:emit": "tsc",
    "build": "npm run typecheck && npm run build:emit",
    "emit:demo": "node dist/examples/treasury-agent/emit-demo.js",
    "test:vectors": "npm run build && node tests/policy-v1-vectors.test.js",
    "test:negative": "npm run build && node tests/replay-divergence-negative.test.js",
    "test:all": "npm run build && npm run test:vectors && npm run test:negative && npm run emit:demo",
    "demo": "./demo.sh",
    "dashboard": "echo 'dashboard-lite not implemented yet' && exit 2",
    "verify:base": "echo 'BASE_WITNESS_NOT_IMPLEMENTED_v0.1' && exit 2"
  },
  "keywords": [
    "ALMS",
    "replay",
    "receipts",
    "constitutional-replay",
    "local-first",
    "base-witness-only"
  ],
  "author": "JayWisdom.eth",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
