# qwen-replay-adapter

Phase 0 JSONWisdom receipt wrapper for Qwen-compatible tools.

```bash
python -m pip install -e '.[test]'
pytest -q
python scripts/generate_manifest.py
python scripts/verify_manifest.py
```

This repository makes no production-concurrency claim. QV_006 is deferred.
