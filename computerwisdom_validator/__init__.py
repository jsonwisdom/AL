"""ComputerWisdom validator package.

This package keeps validator execution, verification, and receipt generation
separate. Importing the package performs no validation and creates no authority.
"""
