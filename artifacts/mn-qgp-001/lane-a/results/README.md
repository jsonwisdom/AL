# Results — [PLACEHOLDER]

This directory will contain **final bound results**:

- `global_ground_state.json` — energy value + component minima
- `component_ground_states.json` — per‑component optimal assignment

**No results are present in this draft.**  
Results are blocked until enumeration is executed and verified.
