# Derived — [PLACEHOLDER]

This directory will contain **derived graph statistics** after the BQM is frozen:

- `graph_statistics.json` — component count, degree histogram, coupler count
- `component_decomposition.json` — mapping of each variable to its K₄ or K₅ component

**No derived data is present in this draft.**  
All files must be computed deterministically from the frozen BQM bytes.
