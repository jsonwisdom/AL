# CVD Receipts

Only observed evidence belongs here.

Expected receipts:

- source commit SHA;
- test command and output;
- GitHub Actions run ID and conclusion;
- workflow job and step results;
- matrix-to-test coverage record;
- recovery-condition verification;
- later, runtime attestation evidence.

Claims without accessible artifacts remain `UNVERIFIED`.
