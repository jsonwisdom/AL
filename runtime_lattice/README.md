# Runtime Lattice
Doctrine:
> No runtime behavior fork without prior provenance.

If the mutation record timestamp is equal to or later than the first affected query timestamp, the deployment is dead.

**Prior provenance or dead deployment.**
