# Hybrid Alabama Warfare — Test Directory Contract v0.1

Authority created: `false`

Admitted:
- deterministic conformance tests
- negative tests for semantic widening
- topology/manifest validation
- replay invariants

Required failure boundaries:
`PASS != CLAIM_TRUE`
`PROVEN != GUILT`
`STORY != TESTIMONY_VERIFIED`
`FACT_LANE != CAUSALITY`
`PUBLIC_RECORD != COMMAND_AUTHORITY`

A passing test proves only the bounded property named by that test.
