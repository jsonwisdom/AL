STATUS: ACTIVE

# Receipt Rules

Do not invent:
- commit hashes
- transaction hashes
- attestations
- contract addresses
- command outputs
- settlement evidence

A receipt is promotable only when visible evidence exists.

Missing evidence must resolve to `NEEDS_SOURCE`.
