STATUS: ACTIVE

# Verification Boundaries

Prompt rules reduce drift and make failure easier to detect.

They do not:
- prove truth
- create cryptographic guarantees
- establish anchors automatically
- replace schema validation
- replace replay verification

If evidence is missing, classify the result as `NEEDS_SOURCE`.
