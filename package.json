{
  "name": "transport-constitution",
  "version": "0.2.0",
  "description": "Bounded truth survives translation pressure.",
  "type": "module",
  "scripts": {
    "test:constitutional": "npx playwright test transport-constitution/indexeddb-smoke.test.spec.js --project=chromium"
  },
  "dependencies": {
    "@noble/hashes": "^1.3.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "serve": "^14.2.0"
  }
}
