name: ALMS Daily JSON Audit

on:
  schedule:
    - cron: "17 6 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate ALMS production receipts
        run: bash scripts/audit/validate_receipts.sh

      - name: Build ALMS receipt ledger
        run: bash scripts/ledger/build_receipt_ledger.sh

      - name: Verify ALMS receipt ledger
        run: bash scripts/ledger/verify_receipt_ledger.sh

      - name: Build daily JSON audit report
        run: bash scripts/audit/build_ipfs_daily_json_report.sh

      - name: Verify daily JSON audit report deeply
        run: bash scripts/audit/verify_ipfs_daily_json_report.sh --deep

      - name: Commit audit report
        run: |
          git config user.name "alms-audit-bot"
          git config user.email "actions@github.com"
          git add _truth/ledger _truth/audit docs/audit
          git commit -m "Add daily ALMS JSON audit report" || exit 0
          git push
