# Constitutional Agent v0.1.0

One replayable execution cell proving bounded authority, refusal telemetry, anchor-gated coverage, and validator-enforced receipts.

Doctrine: boring infrastructure that replays.

Core path: manifest -> execution -> receipt -> anchor -> validation -> pricing.

Protected ratio: 12 invariants / ~100 validator lines / 3 demo cases / 1 mental model.

Merge rule: If you cannot replay the change, you cannot merge it.

Settlement rule: No anchor = no coverage.

Run:
python scripts/demo.py

Expected:
CONSTITUTIONAL EXECUTION CELL: PASS
