# Scope: AL checked-in bytes replay

This directory is a **declared status surface** for one replay only:

```text
scope = checked-in AL Act 2025-251 PDF bytes
plus claim / gate / snapshot / CI witnesses
```

It does not replace `latest_anomaly_leads.jsonl`, `latest_sweep_summary.json`, or `latest_anomaly_summary.json`.

Those statewide receipts remain historical. This lane does not rewrite them.

```text
CLAIM                    = ANOMALY_LEAD_ONLY
PUBLIC_CONTENT_CLAIM     = BLOCKED_PENDING_HUMAN_REVIEW
NO_FAKE_GREEN            = ACTIVE
authority                = false
fraud_verdict            = false
network_fetch            = false
```
